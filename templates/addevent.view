{% extends "main.view" %}
{% block breadcrumb %}
    <a href="/">home</a> <span>&gt;</span> <a href="/evenement_toevoegen/">evenement toevoegen</a>
{% endblock %}
{% block content %}
    {% if is_authenticated %}
        <h2>Voeg een nieuw evenement toe</h2>
        {% if error %}
        <div>
            {{ error }}
        </div>
        {% endif %}
        <form method="post">{% csrf_token %}
            <div class="fieldWrapper">
                <p><label for="id_event_name">Mijn evenement heet</label>
                {{ form.event_name }}
                <label for="id_event_organizer">en wordt georganiseerd door</label>
                {{ form.event_organizer }}.</p>
                <p><label for="id_start_date">Het gaat van start</label>
                <span class="datepicker">{{ form.start_date }}</span>
                <label for="id_start_time">om</label>
                {{ form.start_time }}</p>
                <p><label for="id_end_date">en eindigt</label>
                <span class="datepicker">{{ form.end_date }}</span>
                <label for="id_end_time">om</label>
                {{ form.end_time }}
                <label for="id_event_location">en vindt plaats te</label>
                <span>{{ form.event_location }}</label>.</p>
                <p><label for="id_event_description">Hier is een korte beschrijving van het evenement:</label></p>
                <div>{{ form.event_description }}</div>
            </div>
            <button type="submit">aankondigen</button>
        </form>
    {% else %}
        <p>Je moet inloggen voor je een evenement kan toevoegen. Inloggen kan op de homepagina.</p>
    {% endif %}
{% endblock %}
